{
  "name": "colorpicker",
  "version": "1.0.0",
  "description": "colorPicker: An Ionic project",
  "dependencies": {
    "bower": "^1.3.3"
  },
  "devDependencies": {
    "gulp": "^3.5.6",
    "gulp-htmlmin": "^0.2.0",
    "gulp-minify-css": "^0.3.0",
    "gulp-uglify": "^1.0.1",
    "karma": "^0.12.31",
    "karma-junit-reporter": "^0.2.2"
  },
  "scripts": {
    "start": "node web-server.js",
    "test": "karma start karma.conf.js",
    "test-single-run": "karma start karma.conf.js  --single-run"
  }
}
